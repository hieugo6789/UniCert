export interface WalletDetail {
  walletId: string;
  point: number;
  userId: string;
  depositDate: Date;
  history: string;
  walletStatus: string;
}

export interface currentWallet {
  walletId: number;
  point: number;
  userId: string;
  depositDate: Date;
  history: string;
  walletStatus: string;
}
